import { render, screen } from '@testing-library/react'
import CharacterInfo from './CharacterInfo'

test('renders Breaking Bad Technical Test', () => {

    // render(<CharacterInfo characterData={characterData}/>);
    
});